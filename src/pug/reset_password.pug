extends ./layout/layout.pug

block content
  .container.py-5
    .row.py-2
      .col-7
        h1.mb-3 重新設定新密碼
        .form-group
          label 新密碼
          input.form-control(placeholder="6-12字元，英數混合")
        .form-group
          label 請再輸入一次密碼
          input.form-control(placeholder="密碼需與上一列相同")
        button.btn.rounded-pill.btn-orange 確認更改
    img.fancy-banner(src="images/登入_註冊/login@2x.png")